# Ortak dosya değişiklik talepleri

| parça | dosya | istenen | gerekçe |
|---|---|---|---|
| P04 | `docs/ARCHITECTURE.md` | Olay tablosuna satır ekle: `memory:echo` \| `{day, text, tone, memoryId?, years?, secretId?, charId?}` \| `sim/memory.js` | Dünyanın hatırladığını duyurmanın tek kanalı; sim→ui tek yön korunuyor. |
| P04 | `ui/wait.js` veya `ui/shell.js` | `on('memory:echo', ({text, tone}) => whisper(text, tone))` | Yıldönümü mırıltıları ve sır sızıntıları şu an sadece `S.chronicle`'a düşüyor; ekranda görünmüyor. |
| P04 | `ui/decision.js` | `kicker()` haritasına `echo:'Yankı'` ekle | Yankı olayları geçmişten geldiklerini kicker'da söylesin; şu an "Karar" yazıyor. |
| P04 | `ui/shell.js` | Karakter panelindeki yerel `memoryLines(c,p)` yerine `sim/memory.js`'ten `memoryLines(charId)` kullan; ayrıca `secretLines()` ve `memorySummary()` için bir "Hafıza" paneli | Saf fonksiyonlar hazır: tarih (`when`/`exact`/`ago`), ton, ağırlık, kim, nerede alanlarını döner. Sırların yaşı ve bugünkü şantaj fiyatı `secretLines()`'ta. |
| P04 | `sim/decision.js` | `recallMemory` gövdesini `sim/memory.js`'ten yeniden ihraç et (`export { recallMemory } from './memory.js'`) | Oradaki sürüm ağırlık × olgunluk × yorgunluk × dinlenme ile tartıyor; aynı hatıra üst üste geri gelmiyor. Mevcut sürüm sadece `weight`e bakıyor. |
| P04 | `sim/realm.js` (`realmLevy`) veya `sim/war.js` | `c.retinue` (düz sayı) askerin üstüne eklensin | `content/echoes.js`'teki "gönüllüler" yankısı `me.retinue += n` yazıyor; kimse okumazsa oyuncuya söylenen etki yalan olur. |
| P08 | `src/main.js` | `wireDeath()` / `showDeath()` / `overlayBusy()` / `whenClear()` kaldırılsın; `assumeHeir` importu da gitsin. Ölüm sahnesini artık `ui/succession.js` üstleniyor (`player:died` + `dynasty:extinct` dinliyor, devri kendi ekranından `assumeHeir()` ile yapıyor). | İki ekran üst üste açılıyor; şu an `ui/succession.js` main'inkini geçici bir shim ile ekrandan kaldırıyor. Shim ancak wireDeath silinince gidebilir. |
| P08 | `docs/ARCHITECTURE.md` | Olay tablosuna iki satır: `dynasty:extinct` \| `{reason, day, deadId, name, age, dynasty, usurperId?, reignYears, reigns, generation}` \| `sim/succession.js` — ve `succession:law` \| `{law, state:'sealed'\|'active', doneDay?}` \| `sim/succession.js` | Hanedanın sonu artık `player:died` ile duyurulamıyor (varis yok, payload'da heir olmaz); veraset kanunu değişimi de UI'ya haber olmak zorunda. |
| P08 | `sim/tick.js` | `yearly()` veya `tickDay()` içine `tickSuccession(day)` çağrısı (`import { succeed, tickSuccession } from './succession.js'`) | Mühürlenmiş veraset fermanının yürürlüğe girmesi şu an UI'nin `clock:day` dinleyicisine bağlı; dünya UI olmadan da ilerlemeli. |
| P08 | `src/main.js` | `window.__advance` döngüsündeki `break` koşuluna `|| S.gameOver` eklensin | Hanedan bittikten sonra `__advance` ölü bir oyuncuyla dünyayı ilerletmeye devam ediyor. |
| P03 | `ui/succession.js` (P08) | Ölüm ekranını kendi HTML'inle çizmek yerine `import { stageMoment } from './reveal.js'` kullan: `stageMoment({weight:1, targetId:deadId, scene, outcome:{success:false, knell:true, beat:'bitti', title:'… Öldü', text:'…', effects:[…]}}).then(() => { assumeHeir(); refreshTop(); resume(); })` | Oyuncunun kendi ölümü oyundaki en ağır an; P03'ün sahneleme dili (sessizlik → nefes → tek kelime → cümle → sonuçlar tek tek → 1.5 sn kilitli buton, portre griye dönüyor) hazır ve ihraç edildi. İki ayrı ölüm dili olmasın. |
| P03 | `ui/decision.js` | `initDecisionUI`'daki `on('decision:offered', d => { if (!current) show(d) })` koşuluna bir de `&& !document.querySelector('#revealRoot .rv')` ekle; sahne kapanınca bekleyen kararı göster | Bir sonuç sahnelenirken sim yeni bir karar açabiliyor ve karar sayfası açığa çıkışın üstüne biniyor. P03 şimdilik z-index ile kendini korudu ama doğru çözüm kararın beklemesi. |
| P09 | `ui/shell.js` | "Tebaa" düğmesi (`#btnRealm`) `showRealm()` yerine `ui/realm.js`'ten `openScreen()` çağırsın (`import { openScreen } from './realm.js'`) | Şu an `showRealm()` sadece isim+sayı listesi basıyor; görüş dökümü, fraksiyon üyeliği, askerî güç ve tebaa araçları `ui/realm.js`'te. Geçici çözüm olarak P09 düğmeyi capture-phase'de yakalıyor; entegrasyonda bu kaldırılmalı. |
| P09 | `docs/ARCHITECTURE.md` | Olay tablosuna satırlar: `faction:formed` / `faction:known` / `faction:joined` / `faction:shrank` / `faction:demand` / `faction:revolt` / `faction:exposed` / `faction:resolved` \| `{faction}` \| `sim/factions.js` — ve `faction:signal` \| `{factionId, text, tone}` | Fraksiyon sızıntıları ve ültimatomlar sim→ui tek yönlü kanaldan geçiyor; tablo eksik kalmasın. |
| P09 | `sim/realm.js` | `incomeOf()` ve `realmLevy()` içindeki `v.opinions?.[charId]` yerine `opinion(v.id, charId)` kullanılsın | Şu an hatıralar (`remember()`) vergiyi ve askeri hiç etkilemiyor: bir vassalı yıllarca kızdırıp aynı vergiyi almaya devam ediyorsun. Fraksiyon gücü de bu yüzden gerçek sadakati yansıtmıyor. |
| P09 | `sim/world.js` | Oyuncuya bir düklük (ya da en az 2 vassal) verilsin — `openingSituation`'a "senin de tebaan var" maddesi | Oyuncu şu an sadece kont; `vassalsOf(playerId)` boş, dolayısıyla kendisine karşı fraksiyon kurulamıyor. P09'un yarısı (ültimatom, ahitname, zincir) ancak tebaası olan bir hükümdarda çalışıyor. Şimdilik yalnızca "efendine karşı kurulan fraksiyona davet" kolu doğal olarak tetikleniyor. |
| P09 | `ui/style.css` | (talep yok) | P09 kendi CSS'ini `css('p09-realm', …)` ile enjekte ediyor. |
| P19 | `sim/decision.js` | `offer()` seçenek eşlemesine `confirm: o.confirm \|\| null` ekle | `ui/decision.js` kapıda `o.confirm`'e bakıyor ama `offer()` alanı kopyalamıyor; yazılmış onay cümleleri ("Kendi kardeşini zincire mi vuracaksın?") ekrana hiç çıkmıyor, jenerik metne düşüyor. |
| P19 | `sim/decision.js` | `offer()` seçenek eşlemesine `hiddenMod: o.hiddenMod \|\| 0` ekle | `commit()` zaten `opt.hiddenMod` okuyor ama alan kopyalanmadığı için hep 0. Gösterilen ihtimalin yalan söyleyebilmesi (işaretlerin yanıltması) buna bağlı. |
| P17 | `ui/shell.js` | Karakter panelinde "Sana bakışı" satırının altına `import { aiIntent } from '../sim/ai.js'` ile bir "Niyeti" satırı: `aiIntent(c.id)` null değilse göster ("senin toprağını istiyor", "seni tartıyor", "bir defter tutuyor"…) | Komşuların bir gündemi var ama oyuncu onu yalnızca ültimatom geldiğinde öğreniyor. Casus becerisiyle zaten sızdırılan bilgi panelde de okunabilmeli; aksi hâlde 81 hükümdar hâlâ mobilya gibi duruyor. |
| P17 | `ui/wait.js` | `clock:day` dinleyicisinde `S.chronicle`'ın son elemanı `kind==='rumor'` ise `whisper(text, tone)` çağrılsın (son gösterilen indeks `ui/wait.js` içinde tutulur) | AI'ın sızdırdığı söylentiler şu an yalnızca Vakayiname panelinde duruyor; oyuncu paneli açmadan sınırdaki hazırlığı hiç görmüyor. `decision:tell` P02'nin kanalı olduğu için sim tarafından kullanılmadı. |
